// fonts.js
import { Viga,Poppins } from 'next/font/google'

export const viga = Viga({
  weight: '400',
  subsets: ['latin'],
});


export const poppins = Poppins({
  weight: '400',
  subsets: ['latin'],
});
